# 2026-09-21

## Two-Wheeler SOS Map — expanded 125 → 137 real listings

User: "search google and google maps to add more places and spots with contact numbers, like
local bike repair shops, more towing services and more local services. Add only real spots
and if possible with contact numbers. Do not keep any seed data."

### Sources that worked

**`bizdir24.com` — the find of the session.** Its Kolkata "Motorcycle Repair Shops" listing
page shows names and full addresses, and each record's *detail* page exposes a real phone
number. This is the only source I found that gives verified numbers for small independent
neighbourhood garages. URL shape:
`bizdir24.com/s/in/motorcycle-repair-shops-in-kolkata-wb` → detail pages at
`bizdir24.com/v/in/<category>/<base64-ish-id>`.

**Operators' own websites** for towing. `towmigo.com/kolkata` publishes +91 84472 74918;
`onroadsindia.com/roadside-assistance/kolkata` publishes the toll-free 1800 313 7644 (and
pointed me at **1033**, the NHAI highway helpline — now in the emergency panel).

### Sources that dead-ended (do not retry)

| Source | Problem |
|---|---|
| Sulekha | 403 Forbidden (Azure App Gateway) |
| IndiaMART dir pages | Mask numbers behind "View Mobile Number"; only shows "Kolkata", no street address |
| Infoisinfo | Numbers behind a "Show phone number" button. Does give lat/lng though |
| cardekho tyre dealers | Names only, no numbers printed |
| Grotal | JS/captcha gated |
| Google Maps place pages | Not fetchable |
| `dealers.heromotocorp.com` | CloudFront 403 |
| kolkata.idbf.in | HTTP 500 |
| solve24.in | Empty category stub |

### Added

11 independent workshops (`brand: 'LOCAL SHOP'`) — Behala ×2, Park Circus, Rafi Ahmed Kidwai
Road, Santoshpur, Regent Estate, Shyambazar, Kasba, Netaji Nagar, Tiljala, Barisha. Plus
ONROADS INDIA. Also filled in TOWMIGO's previously-blank number.

**Final: 137 listings, 136 callable, 83 localities.** Only Honda BigWing Topline South has no
published number.

### App changes

- `BRAND_SHORT` gained `'LOCAL SHOP': 'LOCAL'` so the tile label fits.
- Emergency panel gained **1033** (NHAI highway breakdown helpline) — 6 dials now.
- The rail is now SHOW ALL + 2 categories + a divider + **10 brand tiles**
  (8 makers + TOWING + LOCAL). Still fits as a horizontal scroller.

### Gotcha worth remembering

Two syntax errors from my own generator, both at the *join* between old and new records:
1. The last existing record had no trailing comma before the appended block — had to insert
   one at the join point.
2. My emitter wrote `src: 'Operator's own website'` with a **bare apostrophe** because the
   Python string wasn't escaped. `node --check` caught both immediately — always run it on
   data.js after any scripted edit.

### Note

The dev server (`python -m http.server 8777`) had silently died since the last session and was
returning 502 — the browser check failed with `SOS_DATA undefined`. Restart it before
verifying, and check `curl -o /dev/null -w "%{http_code}"` first rather than assuming a code
regression.

---

## Later same day — Google Maps IS fetchable. 137 → 170 listings

The user pushed back: *"aren't you able to add more local spots from google maps?"* I had
previously told them Maps place pages weren't fetchable. **I was wrong.** Worth re-testing an
assumption before repeating it.

### The finding

`https://www.google.com/maps/search/<query>` — plain URL, **no query parameters** — returns a
fully readable server-rendered result list via WebFetch. Each result gives:

- business name
- category (Motorcycle repair shop / Tyre shop / Towing service / Battery store …)
- street address
- phone number
- **opening hours** ("Open 24 hours", "Open · Closes 7 PM")
- **exact coordinates** — in the place URL as `!3d<lat>!4d<lng>`

Caveats:
- **Adding `?hl=en` triggers a Google cookie-consent interstitial** and you get nothing. The
  bare URL works; the response language varies (Lao, Spanish, German, Arabic, Japanese…) but
  names, addresses, phones and coordinates are language-independent. Only the "Open/Closes"
  words need translating — the times come through numerically in the Japanese locale.
- **Roughly half the requests still hit the consent wall.** Just retry, ideally with a
  reworded query. It is intermittent, not a hard block.

Queries that paid off: `bike repair shop kolkata`, `bike repair kolkata`,
`tyre puncture shop kolkata`, `two wheeler towing service kolkata`,
`two wheeler spare parts kolkata`, `bike battery shop kolkata`, `bike service centre kolkata`.

### Added 33 records

12 more independent workshops, 7 tyre/puncture shops, 6 towing operators, 3 spare-parts
shops, 5 battery dealers (Exide/Amaron). Two new categories: **`battery`** (new colour
`#FF2D95`) and `air` relabelled to **TYRE / AIR / PUNCTURE**.

### Honest hours handling

Maps reports "Open · Closes 7 PM" — a closing time but **not an opening time**, and only
true at capture time. A closing time alone cannot tell you whether a place is open now. So:

- `hours: { o: 0, c: 24 }` **only** where Maps says "Open 24 hours" — complete and accurate.
  13 records. They show a green dot and OPEN 24 HOURS.
- Everything else keeps `hours: null` → yellow dot, `CALL AHEAD`. The closing time goes into
  the note as a listing fact ("Listed open till 7 PM"), never as a live claim.

### Final

**170 listings, 169 callable, 92 localities.** Sources: manufacturer directories 122, Google
Maps 33, public business directories 11, operators' own sites 4.

### ToS caveat (documented in the README)

Google's terms restrict scraping and bulk redistribution of Maps content. Low-risk for a
personal/community project; needs licensing or re-collection before running at scale. This is
stated in the README so the user can make that call knowingly.

